# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Environment variables
# Critical for security: Do not commit sensitive credentials!
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Build artifacts
# For the client-side React application
client/build/
client/dist/

# Logs
*.log
logs/
*.log.*

# OS generated files
.DS_Store
Thumbs.db

# IDE specific files
.vscode/
.idea/
*.iml
*.ipr
*.iws

# Test coverage reports
coverage/
.nyc_output/

# Dependency lock files (if not managed by package.json/yarn.lock/pnpm-lock.yaml)
# Generally, these should be committed, but sometimes ignored in specific scenarios.
# For this project, we assume they are committed, so not explicitly ignoring them here.
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Other temporary files
*.tmp
*.swp
*~
.#*
.fuse_hidden*
.nfs*